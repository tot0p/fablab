{% macro renderNavItem(navItem) %}
{% if navItem.type == 'file' %}
    <a href="{{ navItem.url }}" class="nav-item {{ 'active' if page.url == navItem.url }}">
        <span class="nav-icon">📄</span>
        {{ navItem.title }}
    </a>
{% elif navItem.type == 'directory' and navItem.children.length > 0 %}
    <div class="nav-dropdown">
        <button class="nav-dropdown-toggle" aria-expanded="false">
            <span class="nav-icon">📁</span>
            {{ navItem.title }}
            <span class="dropdown-arrow">▶</span>
        </button>
        <div class="nav-dropdown-content">
            {% for child in navItem.children %}
                {{ renderNavItem(child) }}
            {% endfor %}
        </div>
    </div>
{% endif %}
{% endmacro %}

{{ renderNavItem(item) }}